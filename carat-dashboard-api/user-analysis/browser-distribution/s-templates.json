{
  "functionName": "${project}-${stage}-${name}",
  "browser-distribution-template": {
    "application/json": {
      "domain": "$input.params('domain')",
      "group": "$input.params('group')",
      "from": "$input.params('from')",
      "to": "$input.params('to')",
      "device": "$input.params('device')"
    }
  }
}